spring:
  profiles:
    active: @profiles.active@

servlet:
  context-path: /

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: io.github.hulang1024.chinesechessserver.database.entity
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

